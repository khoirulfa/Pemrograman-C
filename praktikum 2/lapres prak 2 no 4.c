#include <Stdio.h> //Header file salah penulisannya seharusnya <stdio.h>
main()
{
    float a, b, c; //function main tidak dideklarasi dengan type data
    a = 3.0;
    b = 4.0;
    c = a * a + b * b;       //tidak ada semicolon
    printf("C * C = %f", c); //salah karena mengunakan persen f untuk menampikan
    Bilangan float / pecahan
}
